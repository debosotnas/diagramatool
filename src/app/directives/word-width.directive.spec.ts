import { WordWidthDirective } from './word-width.directive';

describe('WordWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new WordWidthDirective();
    expect(directive).toBeTruthy();
  });
});
